<html>
<body>
  <h1>Genres</h1>
  <p><%= @genres.count %></p>
  <ol>
    <% @genres.each do |genre| %>
      <li>
        <p><a href="genres/<%= genre.name.gsub(' ', '_') %>.html"><%= genre.name %></a></p>
      </li>
    <% end %>
  </ol>
</body>
</html>